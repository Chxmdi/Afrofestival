# --- Client (Vite) — safe to expose, protected by row-level security ---
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# --- Edge Function secrets (set with `supabase secrets set ...`) --------
# Provide at least one search provider for live sponsor discovery.
# When neither is set, manual URL + CSV import still work.
# TAVILY_API_KEY=tvly-xxxxxxxx
# SERPER_API_KEY=xxxxxxxxxxxxxxxx
